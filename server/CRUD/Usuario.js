import {supabase} from '../dataBase.js';

/* async function signInWithEmail(email, password) {
    const { data, error } = await supabase.auth.signUp({
        email: email,
        password: password,
    });
    if (error) console.log("Error logging in with Email::::::::::::::::::::", error);
}


export {signInWithEmail} */